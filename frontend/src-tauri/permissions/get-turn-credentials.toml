[[permission]]
identifier = "allow-get-turn-credentials"
description = "Fetch real, time-limited TURN relay credentials from the backend for a remote assist WebRTC connection"
commands.allow = ["get_turn_credentials"]
